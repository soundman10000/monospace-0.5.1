# Target database. The runner creates the database if it does not exist.
DATABASE_URL=postgres://postgres:monospace@localhost:5434/benefits

# Optional. Used only when DATABASE_URL's database needs to be created.
ADMIN_DATABASE_URL=postgres://postgres:monospace@localhost:5434/postgres
